server {
    listen 80;
    server_name operation.einvoice.local;
    root /var/www/html/einvoice/einvoice-operation/public;

    index index.php index.html index.htm;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass einvoice-operation:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.ht {
        deny all;
    }

    error_log /var/log/nginx/einvoice_operation_error.log;
    access_log /var/log/nginx/einvoice_operation_access.log;
}

server {
    listen 80;
    server_name backend.einvoice.local;
    root /var/www/html/einvoice/kv-einvoice-backend/public;

    client_max_body_size 100M;

    index index.php index.html index.htm;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass einvoice-backend:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.ht {
        deny all;
    }

    error_log /var/log/nginx/einvoice_backend_error.log;
    access_log /var/log/nginx/einvoice_backend_access.log;
}

server {
    listen 80;
    server_name kafka-site.einvoice.local;
    root /var/www/html/einvoice/kv-kafka-einvoice-site;

    index index.php index.html index.htm;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass einvoice-kafka-site:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.ht {
        deny all;
    }

    error_log /var/log/nginx/einvoice_kafka_site_error.log;
    access_log /var/log/nginx/einvoice_kafka_site_access.log;
}

server {
    listen 80;
    server_name search-api.einvoice.local;
    root /var/www/html/einvoice/kv-einvoice-search-api;

    index index.php index.html index.htm;

	location / {
		rewrite /v1/(.*)$ /v1/api.php?request=$1 last;
		rewrite /service/nhanHoaDon$ /service/nhanHoaDon.php last;
		rewrite /service/lostpass$ /service/lostpass.php last;
		if (!-e $request_filename){
			rewrite ^.*$ /index.php last;
		}
		rewrite !\.(htm|txt|swf|js|ico|gif|jpg|JPG|png|css|php|xml|rar|doc|prc|docx|bmp|xls|xlsx|vsd|rpt|pdf|ppt|pps|rtf|cdf|DOC|ttf|woff|woff2|svg|eot|jar|zip|map)$ /index.php;
		try_files $uri $uri/ /index.php;
	}

    location ~ \.php$ {
        fastcgi_pass einvoice-search-api:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.ht {
        deny all;
    }

    error_log /var/log/nginx/einvoice_search_api_error.log;
    access_log /var/log/nginx/einvoice_search_api_access.log;
}

server {
    listen 80;
    server_name file.einvoice.local;
    root /var/www/html/einvoice/kv-file-manager;

    index index.php index.html index.htm;

	location / {
		if (!-e $request_filename){
			rewrite ^.*$ /index.php last;
		}
		rewrite !\.(htm|txt|swf|js|ico|gif|jpg|JPG|png|css|php|xml|rar|doc|prc|docx|bmp|xls|xlsx|vsd|rpt|pdf|ppt|pps|rtf|cdf|DOC|ttf|woff|woff2|svg|eot|jar|zip|map)$ /index.php;
		try_files $uri $uri/ /index.php;
	}

    location ~ \.php$ {
        fastcgi_pass einvoice-file:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.ht {
        deny all;
    }

    error_log /var/log/nginx/einvoice_file_error.log;
    access_log /var/log/nginx/einvoice_file_access.log;
}

server {
    listen 80;
    server_name kfcrm.kfin.local;
    root /var/www/html/kfin/kfcrm;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass kfcrm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    client_max_body_size 100M;

    location ~ /\.(?!well-known).* {
        deny all;
    }

    error_log /var/log/nginx/kfcrm_error.log;
    access_log /var/log/nginx/kfcrm_access.log;
}

server {
    listen 80;
    server_name frontend.einvoice.local;
    root /var/www/html/einvoice/kv-einvoice-frontend-dev;

    location / {
        proxy_pass http://einvoice-frontend:4200;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /sockjs-node {
        proxy_pass http://einvoice-frontend:4200;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    error_log /var/log/nginx/einvoice_frontend_error.log;
    access_log /var/log/nginx/einvoice_frontend_access.log;
}

server {
    listen 80;
    server_name test-kafka.local;
    root /var/www/html/test_kafka/public;

    index index.php index.html index.htm;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass test-kafka:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.ht {
        deny all;
    }

    error_log /var/log/nginx/test_kafka_error.log;
    access_log /var/log/nginx/test_kafka_access.log;
}
